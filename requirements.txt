flask>=3.0
pyyaml>=6.0
requests>=2.31
waitress>=3.0
psutil>=5.9

# Agent module (Phase 1)
tiktoken>=0.7            # Token counting for context management

# MCP (Model Context Protocol) client for external tools
mcp>=2.0

# Scheduled tasks (定时主动任务)
apscheduler>=3.10
